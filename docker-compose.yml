version: '3.8'

services:
  export-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quckapp-export-service
    ports:
      - "5015:5015"
    environment:
      - PORT=5015
      - DATABASE_URL=mysql+asyncmy://quckapp:quckapp@mysql:3306/quckapp_exports
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - LOCAL_STORAGE_PATH=/tmp/exports
    depends_on:
      - mysql
      - redis
    volumes:
      - export-data:/tmp/exports
    networks:
      - quckapp-network
    restart: unless-stopped

  mysql:
    image: mysql:8.0
    container_name: quckapp-export-mysql
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=quckapp_exports
      - MYSQL_USER=quckapp
      - MYSQL_PASSWORD=quckapp
    ports:
      - "3317:3306"
    volumes:
      - export-mysql-data:/var/lib/mysql
    networks:
      - quckapp-network

  redis:
    image: redis:7-alpine
    container_name: quckapp-export-redis
    ports:
      - "6389:6379"
    networks:
      - quckapp-network

networks:
  quckapp-network:
    driver: bridge

volumes:
  export-mysql-data:
  export-data:
